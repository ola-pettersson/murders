We analyze US murder data collected by the FBI.

download-data.R - downloads file to data directory

wrangle-data.R - creates a derived dataset and saves as R objects in rda directory

analysis.R - generates a plot and saves it to the figs directory